#include <iostream>
using namespace std;

int main() {
    int X, N;
    cin >> N >> X;
    
    int *arr;
    arr = new int[N];
    
    for(int i = 0; i < N; i++) {
        cin >> arr[i];
    }
    for(int i = 0; i < N; i++) {
        if(arr[i] < X) cout << arr[i] << ' ';
    }
    
    delete[] arr;
}
