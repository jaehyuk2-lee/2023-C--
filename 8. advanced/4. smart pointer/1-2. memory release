#include <iostream>
#include <memory>
using namespace std;

int main() {
    unique_ptr<int> a(new int(5));
    unique_ptr<int> b(a.release()); //소유권 이전하며 a 연결 끊기
    
    cout << *b << endl;
}

//unique_ptr은 1대1 대응이다
//메모리 소유권 : 메모리 해제 의무
