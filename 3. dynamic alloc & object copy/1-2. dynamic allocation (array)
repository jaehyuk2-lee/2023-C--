#include <iostream>
using namespace std;

int main() {
    int *arr;
    int len;
    
    cout << "배열의 크기 : ";
    cin >> len;
    
    arr = new int[len];
    for(int i = 0; i < len; i++){
        arr[i] = i + 1;
        cout << arr[i] << endl;
    }
    
    delete[] arr;
}
